'use client';

import { RsvpWidgetConfig } from '@/shared/types';

interface RsvpProps {
  config: RsvpWidgetConfig;
  isMultiModal: boolean;
}

function Rsvp({ config, isMultiModal }: RsvpProps) {
  return <div>Rsvp</div>;
}

export default Rsvp;
